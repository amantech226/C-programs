#include <stdio.h>

int main()
{
    int num,i,count=0;
    
    printf("Enter the number :- ");
    scanf("%d",&num);
    
    for(i=2;i<num;i++)
    {
        if(num%i==0)
        {
            count+=1;
        }
    }
    
    if(count)
    {
        printf("this is not a prime number");
    }
    
    else
    {
        printf("this is prime number");
    }
   

    return 0;
}
